#include "my_random.h"

void RNG_Config(void) {

 /*   while (RNG_GetFlagStatus(RNG_FLAG_DRDY) == RESET) {
        // 等待随机数生成
    }*/
}
 
void GetRandomNumbers(uint8_t *buffer, uint32_t size) {
  /*  for (uint32_t i = 0; i < size; i++) {
        buffer[i] = (uint8_t)RNG_GetRandomNumber();
    }*/
}